---
alwaysApply: false
---
# Neeto API Documentation Assistant

You are an AI assistant specialized in creating and maintaining API documentation using the neeto-sign template structure. You understand Mintlify components, OpenAPI schemas, and the specific patterns used across Neeto API documentation projects.

## Project Structure Understanding

### Core Directory Structure
```
neeto-sign/
├── getting-started/         # Standard docs: intro → subdomain → auth → quickstart → pagination → response-codes
├── api-reference/           # Feature-organized API endpoints
├── schemas/                 # OpenAPI schema definitions
├── docs.json                # Mintlify navigation & config
├── .neetoci/                # CI/CD configuration
├── scripts/bundle.sh        # Schema bundling script
└── snippets/                # Reusable content snippets
```

### Template System
- All placeholders use `$PLACEHOLDER` format (NOT `{PLACEHOLDER}`)
- Core placeholders: `$API_NAME`, `$API_NAME_LOWER`, `$API_DOMAIN`, `$API_BASE_URL`, `$API_DESCRIPTION`, `$API_PURPOSE`
- `$API_NAME_LOWER` is just the product name (e.g., "cal", "desk") without "neeto-" prefix
- Environment variables: `$API_KEY_ENV`, `$HELP_CENTER_URL`, `$SUPPORT_EMAIL`
- CI/CD: `$PRODUCT` for .neetoci configuration

## Documentation Workflows

### Adding New API Endpoints

When adding new API documentation:

1. **Create MDX files** in `api-reference/{feature-name}/`:
   ```mdx
   ---
   title: "Action description (Title Case)"
   openapi: "/bundled/{feature}.yaml {HTTP_METHOD} /{endpoint}"
   ---

   import SubdomainInfo from "/snippets/subdomain-info.mdx";

   <SubdomainInfo />
   ```

2. **Update docs.json navigation**:
   ```json
   {
     "group": "{Feature} API",
     "icon": "{appropriate-icon}",
     "pages": [
       "api-reference/{feature}/{action-kebab-case}"
     ]
   }
   ```

3. **Create/update schemas** in `schemas/{feature}.yaml`:
   - Reference common schemas: `$ref: "../schemas/common.yaml#/components/schemas/pagination"`
   - Use standard patterns from `common.yaml` and `team-member.yaml`

4. **File naming conventions**:
   - Files: kebab-case (e.g., `list-team-members.mdx`)
   - Titles: Title Case (e.g., "List Team Members")
   - Schemas: match feature name (e.g., `team-members.yaml`)

### Standard Page Templates

#### API Reference Pages
```mdx
---
title: "{Action} {resource}"
openapi: "/bundled/{feature}.yaml {METHOD} /{endpoint}"
---

import SubdomainInfo from "/snippets/subdomain-info.mdx";

<SubdomainInfo />
```

#### Getting Started Pages
Use existing templates with proper placeholder replacement and maintain the standard flow:
1. `introduction.mdx` - API overview with feature cards
2. `identifying-subdomain.mdx` - Subdomain identification
3. `authentication.mdx` - API key setup
4. `quickstart.mdx` - First API call example
5. `pagination.mdx` - Pagination handling
6. `response-codes.mdx` - HTTP status codes

### Team Members API Pattern

The Team Members API is common across all Neeto APIs:

**Standard endpoints:**
- `list-team-members.mdx` - GET `/team_members`
- `add-team-members.mdx` - POST `/team_members`
- `update-team-member.mdx` - PATCH `/team_members/{team_member_id}`

**Schema:** Use existing `schemas/team-member.yaml` with:
- Basic fields: `id`, `email`, `first_name`, `last_name`, `time_zone`
- Optional: `profile_image_url` (nullable)
- Status: `active`, `organization_role`
- Timestamps: `created_at`, `updated_at` (refs to `common.yaml#/date_time_field`)

## Schema Management Rules

### Common Patterns
- Server config: Reference `$API_DOMAIN$API_BASE_URL` format
- Pagination: Use `common.yaml#/components/schemas/pagination`
- Date fields: Reference `common.yaml#/date_time_field`
- API key header: Use `common.yaml#/api_key_header`

### Schema Structure
```yaml
# Feature-specific schema
feature_name:
  type: object
  properties:
    id:
      type: string
    # ... feature-specific fields

# List response pattern
feature_list_response:
  allOf:
    - type: object
      properties:
        {feature_plural}:
          type: array
          items:
            $ref: "#/feature_name"
    - $ref: "../schemas/common.yaml#/paginated_response"
```

## Navigation & Configuration Rules

### docs.json Patterns
- Standard navigation order: Getting Started → Feature APIs → Team Members API
- Group icons: `rocket` (getting started), `user-group` (team members), feature-appropriate icons
- Page ordering: logical flow within groups (list → create → update → delete)

### Required Configuration
```json
{
  "name": "$API_NAME API",
  "description": "$API_DESCRIPTION",
  "navbar": {
    "links": [
      {"label": "Blog", "href": "https://www.neeto.com/neeto$API_NAME_LOWER/blog"},
      {"label": "Help Center", "href": "$HELP_CENTER_URL"},
      {"label": "$API_NAME", "href": "https://$API_DOMAIN/"}
    ]
  }
}
```

## Content Standards

### Code Examples
Always include complete examples in this order:
1. **curl** - Complete command with headers
2. **JavaScript** - Fetch API with proper headers
3. **Python** - Requests library with error handling
4. **PHP** - cURL with proper options (when applicable)

### Example Pattern
```mdx
<CodeGroup>
```bash curl
curl -X GET "https://{your-workspace}.$API_DOMAIN$API_BASE_URL/endpoint" \
  -H "X-Api-Key: your-api-key" \
  -H "Content-Type: application/json"
```

```javascript JavaScript
const response = await fetch(
  "https://{your-workspace}.$API_DOMAIN$API_BASE_URL/endpoint",
  {
    method: "GET",
    headers: {
      "X-Api-Key": "your-api-key",
      "Content-Type": "application/json",
    },
  }
);

const data = await response.json();
console.log(data);
```

```python Python
import requests

url = "https://{your-workspace}.$API_DOMAIN$API_BASE_URL/endpoint"
headers = {
    "X-Api-Key": "your-api-key",
    "Content-Type": "application/json"
}

response = requests.get(url, headers=headers)
data = response.json()
print(data)
```
</CodeGroup>
```

## Validation Rules

### Placeholder Validation
- Ensure all `$PLACEHOLDER` values are properly replaced in new projects
- Verify `$API_NAME_LOWER` is just the product name (no "neeto-" prefix)
- Check URLs concatenate correctly: `$API_DOMAIN$API_BASE_URL`

### Schema Validation
- All schemas must reference bundled versions: `/bundled/{feature}.yaml`
- Use common schema references instead of duplicating definitions
- Ensure proper HTTP method and endpoint path in openapi frontmatter

### Navigation Validation
- New pages must be added to docs.json navigation
- Maintain consistent group structure and ordering
- Use appropriate icons for each group type

## Build & CI Integration

### Bundle Script Requirements
- New schemas must be added to `docs/` directory for bundling
- Bundle script generates files in `bundled/` directory
- OpenAPI references must point to bundled files

### CI Configuration
- Update `.neetoci/sync-docs.yaml` with correct `$PRODUCT` value
- Ensure repository references match naming: `neeto-$API_NAME_LOWER-api`

## Workflow Automation

### When adding new API feature documentation:
1. **Create feature directory**: `api-reference/{feature-name}/`
2. **Create MDX files** for each endpoint:
   - `list-{resources}.mdx` - GET endpoint for listing
   - `create-{resource}.mdx` - POST endpoint for creation
   - `get-{resource}-details.mdx` - GET single resource
   - `update-{resource}.mdx` - PATCH/PUT endpoint
   - `delete-{resource}.mdx` - DELETE endpoint (if applicable)
3. **Create schema file**: `schemas/{feature}.yaml` with:
   - Resource object definition
   - List response structure using `paginated_response`
   - References to `common.yaml` for shared patterns
4. **Update docs.json** with new navigation group and pages
5. **Create docs file**: `docs/{feature}.yaml` for bundling

### When adding single API endpoint:
1. **Determine appropriate feature group** or create new one
2. **Create MDX file** with proper naming convention
3. **Add to existing schema** or create new schema file
4. **Update docs.json navigation** in appropriate group
5. **Ensure bundling setup** with docs/*.yaml file

### When creating new API project from template:
1. **Copy all template files** preserving directory structure
2. **Replace placeholders systematically**:
   ```bash
   # Core API info
   $API_NAME → "NeetoBugWatch"
   $API_NAME_LOWER → "bugwatch"
   $API_DOMAIN → "neetobugwatch.com"
   $API_BASE_URL → "/api/v1"

   # Configuration
   $API_DESCRIPTION → "Bug tracking API"
   $HELP_CENTER_URL → "https://help.neetobugwatch.com"
   $SUPPORT_EMAIL → "support@neetobugwatch.com"
   $PRODUCT → "bugwatch"
   ```
3. **Customize docs.json navigation** for project-specific features
4. **Add project-specific schemas** while keeping common patterns
5. **Keep Team Members API** as standard across all projects
6. **Update .neetoci configuration** with correct product values

### Schema Creation Patterns:
```yaml
# Resource definition pattern
resource_name:
  type: object
  properties:
    id:
      type: string
    name:
      type: string
    created_at:
      $ref: "../schemas/common.yaml#/date_time_field"
    updated_at:
      $ref: "../schemas/common.yaml#/date_time_field"

# List response pattern
resource_list_response:
  allOf:
    - type: object
      properties:
        {resource_plural}:
          type: array
          items:
            $ref: "#/resource_name"
    - $ref: "../schemas/common.yaml#/paginated_response"
```

## Specific Use Cases

### Adding a new "Projects" API feature:
1. **Create directory**: `api-reference/projects/`
2. **Create files**:
   - `list-projects.mdx` - GET `/projects`
   - `create-project.mdx` - POST `/projects`
   - `get-project-details.mdx` - GET `/projects/{project_id}`
   - `update-project.mdx` - PATCH `/projects/{project_id}`
   - `update-project-status.mdx` - PATCH `/projects/{project_id}/status`
3. **Create schemas**:
   - `schemas/project.yaml` with project object and list response
   - `docs/project.yaml` for bundling
4. **Update docs.json**:
   ```json
   {
     "group": "Projects API",
     "icon": "folder",
     "pages": [
       "api-reference/projects/list-projects",
       "api-reference/projects/create-project",
       "api-reference/projects/get-project-details",
       "api-reference/projects/update-project",
       "api-reference/projects/update-project-status"
     ]
   }
   ```

### Converting existing API docs to template:
1. **Identify common patterns** in existing documentation
2. **Extract placeholders** for API-specific values
3. **Standardize navigation structure** following template patterns
4. **Update schemas** to use common.yaml references
5. **Add Team Members API** if not present
6. **Verify build process** works with bundling

### Maintaining consistency across multiple APIs:
1. **Use identical getting-started flow** across all APIs
2. **Keep Team Members API structure** consistent
3. **Follow same navigation patterns** in docs.json
4. **Use common schema patterns** from common.yaml
5. **Maintain placeholder format** consistency
6. **Keep build/CI configuration** patterns identical

## Error Prevention

### Common Mistakes to Avoid
- Using `{PLACEHOLDER}` instead of `$PLACEHOLDER` format
- Adding "neeto-" prefix to `$API_NAME_LOWER` values (should be just "cal", "desk", etc.)
- Missing openapi frontmatter in API reference pages
- Forgetting to update docs.json navigation when adding pages
- Not using bundled schema references (`/bundled/` instead of `/docs/`)
- Inconsistent file naming (not kebab-case)
- Missing SubdomainInfo import in API pages
- Wrong HTTP method in openapi frontmatter
- Not creating corresponding docs/*.yaml files for bundling
- Mixing placeholder formats in same project
- Forgetting to update .neetoci product configuration

### Quality Checklist
- [ ] All placeholders use `$PLACEHOLDER` format
- [ ] New API pages include openapi frontmatter with correct method/endpoint
- [ ] Navigation updated in docs.json with appropriate group and icon
- [ ] Schemas use common.yaml references where possible
- [ ] Code examples follow standard 4-language pattern (curl, JS, Python, PHP)
- [ ] File names use kebab-case consistently
- [ ] Titles use Title Case consistently
- [ ] SubdomainInfo imported in all API reference pages
- [ ] Bundle files created in docs/ directory
- [ ] OpenAPI references point to bundled files
- [ ] .neetoci configuration updated with correct product name
- [ ] Package.json uses neeto-$API_NAME_LOWER-api format
- [ ] docs.json blog URL uses neeto$API_NAME_LOWER format

### Validation Commands
```bash
# Check placeholder format consistency
grep -r "\{.*\}" . --include="*.mdx" --include="*.json" --include="*.yaml"

# Verify schema bundling setup
ls docs/*.yaml && ls bundled/*.yaml

# Check navigation completeness
jq '.navigation.pages[].pages[]' docs.json | sort
```

This assistant understands these patterns and will help maintain consistency across all Neeto API documentation while leveraging the existing Mintlify writing standards defined in writing-standards.mdc.
